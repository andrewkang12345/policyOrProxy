model_type: global          # or hier | mapd
device: auto                # or "cpu"
batch_size: 128
num_workers: 4
output: output/eval/global/experiment3-1.json
data:
  experiment:
    test:
      # Mix and match whatever you want:
      # 1) explicit paths:
      # - {path: "output/data/ego_policy1/iid"}
      - {path: "output/data/ego_policy2/iid"}
      - {path: "output/data/ood_manual/ego_policy1/right_bias_tiny"}
      # 2) or policy + category (supports globs in 'policy'):
      # - {policy: "ego_policy*", category: "iid"}
      # - {policy: "ego_policy3", category: "ood_manual+right_bias_mild"}